---
interface Item {
  href?: string;
  label: string;
  key?: string;
}

interface Props {
  items: Item[];
}

const { items } = Astro.props;
---

<nav aria-label="Breadcrumb" class="app-breadcrumbs mb-5">
  <ol class="flex items-center gap-2.5 text-sm text-[var(--color-text-soft)]">
    {items.map((item, index) => (
      <>
        <li class="shrink-0">
          {(item.href || (item.key && index < items.length - 1)) && index < items.length - 1 ? (
            <a
              class="inline-flex items-center rounded-full px-2.5 py-1.5 transition hover:bg-[var(--color-surface-soft)] hover:text-[var(--color-text)]"
              href={item.href ?? '#'}
              aria-disabled={item.href ? undefined : 'true'}
              tabindex={item.href ? undefined : -1}
              data-breadcrumb-key={item.key}
            >
              {item.label}
            </a>
          ) : (
            <span
              class="inline-flex items-center rounded-full bg-[var(--color-surface-soft)] px-2.5 py-1.5 font-semibold text-[var(--color-text)]"
              data-breadcrumb-key={item.key}
            >
              {item.label}
            </span>
          )}
        </li>
        {index < items.length - 1 && <li aria-hidden="true" class="shrink-0 text-[var(--color-text-soft)]/70">›</li>}
      </>
    ))}
  </ol>
</nav>
